const { MessageEmbed } = require("discord.js");

module.exports = {
    run: async (client, msg, ags) => {
        
    },

    conf: {},

    get help() {
        return {
            name: "queue",
            description: "lista de músicas",
            usage: "queue",
            category: "music"
        }
    }
}

}